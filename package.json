{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    
  },
  "devDependencies": {
   
  }
}